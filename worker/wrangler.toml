name = "meet-schwerin"
main = "src/entry.js"
compatibility_date = "2026-08-23"
workers_dev = true

[vars]
APP_URL = "https://nexar69.github.io/NVS-meetup-planner/"
PLAN_TTL_SECONDS = "259200"

# The PLANS KV binding is added from the Cloudflare dashboard after the
# first Worker deployment. Keeping the namespace id out of this file lets
# dashboard/GitHub deployments work without a placeholder id.
